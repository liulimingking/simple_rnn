核心步骤：
加载数据：载入 IMDB 数据集。
数据预处理：由于影评长度不一，需要将所有序列填充或截断到相同的长度。
构建模型：定义一个简单的 RNN 模型架构，通常包括嵌入层 (Embedding)、RNN 层 (SimpleRNN) 和一个用于分类的全连接层 (Dense)。
编译模型：为模型指定优化器、损失函数和评估指标。
训练模型：使用训练数据来训练模型。
评估模型：使用测试数据来检验模型的准确率。